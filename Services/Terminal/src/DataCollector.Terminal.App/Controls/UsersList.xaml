<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DataCollector.Terminal.App.Controls.UsersList">
    <CollectionView SelectionMode="Single">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout Padding="5">
                    <BoxView HeightRequest="2" Margin="10" BackgroundColor="{StaticResource StrokeColor}"/>
                    <Label Text="Hello" FontSize="Subtitle" Margin="10"/>
                    <Label Text="Admin" Margin="40, 0, 0, 0"/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentView>