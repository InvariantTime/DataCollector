<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing ="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="DataCollector.Terminal.App.Pages.Scanning"
             Title="Scanning">

    <Grid BackgroundColor="#282828">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <zxing:CameraBarcodeReaderView 
            x:Name="barcodeReader"
            BarcodesDetected="OnBarcodeDetected"
            WidthRequest="150"  HeightRequest="200"/>

        <VerticalStackLayout Grid.Row="1" Grid.Column="0">

            <Button Text="Scanning" BackgroundColor="{StaticResource Key=backColor}" TextColor="{StaticResource Key=textColor}" WidthRequest="200" HeightRequest="50" Margin="20" FontSize="Medium" Clicked="ScanningQR" Grid.Row="1" Grid.Column="0"/>

            <Button Text="Back" BackgroundColor="{StaticResource Key=backColor}" TextColor="{StaticResource Key=textColor}" WidthRequest="200" HeightRequest="50" Margin="20" FontSize="Medium" Clicked="NavigationToPickPage" Grid.Row="1" Grid.Column="0"/>


        </VerticalStackLayout>


    </Grid>

</ContentPage>

 